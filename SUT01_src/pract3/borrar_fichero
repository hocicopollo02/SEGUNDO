argument=$1

if test $argument -f
then
    if test $argument -s
    then 
        rm $argument -f
        echo Se ha borrado el archivo
    else
        echo Es un directorio y no se puede borrar
    fi
fi
